name: gitartwork

on:
  push:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: jasineri/gitartwork@v1
        with:
          user_name: YOUR_USERNAME
          text: YOUR_TEXT

      - uses: jasineri/simple-push-action@v1
        with:
          commit_message: "generate gitartwork"
